# bench3d_adjacent_keys_g4_a4

Right index finger alternates between adjacent `G4` and `A4`.

Expected behavior: each intentional tap should trigger the intended neighboring key once. This clip tests whether 2D key selection remains stable while 3D controls hit timing.
