# bench3d_index_slow_g4

Right index finger slowly taps `G4`.

Expected behavior: each deliberate slow tap should trigger exactly one `G4` hit. This clip is the clean reference for 3D arm, press, and release thresholds.
