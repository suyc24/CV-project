# bench3d_ring_low_lift_e4

Right ring finger naturally taps `E4` with a low lift.

Expected behavior: normal ring-finger taps should trigger without requiring an exaggerated high lift. This clip is important for lowering the 3D armed/drop thresholds safely.
