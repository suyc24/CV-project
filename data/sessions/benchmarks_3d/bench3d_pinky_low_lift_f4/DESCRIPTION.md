# bench3d_pinky_low_lift_f4

Right pinky naturally taps `F4` with a low lift.

Expected behavior: normal pinky taps should trigger once per tap. This clip checks whether the 3D-only detector works for weaker, lower-lift fingers.
